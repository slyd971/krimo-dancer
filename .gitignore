# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# raw video masters (kept locally, not shipped — only the optimized encode in public/videos is served)
public/videos/*.mov

# raw asset dumps — matière brute non triée, non référencée par l'app (déjà reprise dans public/)
/Gallery-Karim/
/La Danse ou Rien/
/La Tempête/
/bio-karim/
